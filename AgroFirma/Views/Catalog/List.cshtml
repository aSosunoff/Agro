@model Model.Infrastructure.PageModel
@{
    ViewBag.Title = "List";
}

<h2>List</h2>

<div class="col-lg-12">
    @foreach (var item in Model.CatalogModel.Ccategories)
    {
        
    }
</div>

<div class="col-lg-12">
    @foreach (var item in Model.CatalogModel.Rstocks)
    {

    }
</div>